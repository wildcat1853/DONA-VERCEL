"use client";
import React from "react";
import { Button } from "../ui/button";

type Props = {};

function CreateProjectButton({}: Props) {
  return <Button onClick={() => {}}>Create new project</Button>;
}

export default CreateProjectButton;
